from prac_08.car import car
from random import randit


class Unreliable(car):


def init(self, name, fuel, reliable):
        super().__init__(name, fuel)
        self.reliable = reliable


def init(self, distance):
    rand_num = randit(1,100)









